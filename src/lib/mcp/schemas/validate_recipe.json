{
  "name": "validate_recipe",
  "description": "Lints markup against the golden rules. Flags class names that are not in the registry, component <style> blocks, and hardcoded pixel values.",
  "parameters": {
    "type": "object",
    "properties": {
      "code": {
        "type": "string",
        "description": "The Svelte or HTML markup to validate."
      }
    },
    "required": [
      "code"
    ]
  }
}
