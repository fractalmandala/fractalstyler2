{
  "name": "css_to_fractals",
  "description": "Converts raw CSS declarations (from Figma/OpenDesign inspection) into fractalstyler2 registry classes, composed in markup. Reports any declaration the registry does not cover.",
  "parameters": {
    "type": "object",
    "properties": {
      "css": {
        "type": "string",
        "description": "Raw CSS block or declaration lines"
      }
    },
    "required": [
      "css"
    ]
  }
}
